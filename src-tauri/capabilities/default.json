{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "add-account"],
  "permissions": [
    "core:default",
    "notification:default",
    "opener:default"
  ]
}
